# Contributing

Please report reproducibility or artifact issues with exact steps and logs.

For changes:
1. Update the relevant tests.
2. Run `python scripts/validate_artifacts.py`.
3. Run `python -m pytest tests/`.
4. Update documentation when interfaces or artifact formats change.
